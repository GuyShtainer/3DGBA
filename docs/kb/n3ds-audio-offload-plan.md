# N3DS multi-core audio-offload + 804MHz detection — implementation plan

_Synthesized by the `n3ds-perf-cores` verified workflow (2026-06-09)._

**STATUS (2026-06-09): IMPLEMENTED, pending hardware sign-off.** In: the 804MHz/L2 detector + splash
warning, the dedicated core-1 audio thread with per-core lock-free SPSC rings (`source/audio.{c,h}`,
worker pump in `worker_main`, `audio_set_params` per frame in `run_session`), and the link mute
lifted. Deviations: pacing uses a 4ms `svcSleepThread` loop (simple + robust; the ~150ms wave-buffer
ring never starves) instead of the ndsp callback; the clock detector is self-calibrating (busy loop
timed before vs after `osSetSpeedupEnable`) so no per-build `D268` constant is needed. Step 0
(instrumentation) was skipped. Builds clean (`.3dsx` + `.cia`). Remaining: real-New-3DS validation
(60fps-with-audio, no core-1 underrun crackle, audio survives a link)._

{
  "summary": "Authoritative N3DS core model + optimal thread/core layout to stop audio halving fps + 804MHz/L2 detection & warning",
  "agentCount": 9,
  "logs": [],
  "result": {
    "plan": "I have everything I need. The code matches the analyses exactly (audio_feed at main.c:425, the linked mute at 414-417, startup at 724-728). Here is the implementation plan.\n\n---\n\n# 3DGBA — Multi-core audio-offload implementation plan\n\nGrounded in the actual code (`source/main.c`, `source/audio.{c,h}`, `source/gbacore.{c,h}`) and the verified libctru decls. The single goal: get the per-frame audio work (`audio_feed`, `main.c:425`) off core 0 and off the frame-critical path, recovering 60fps-with-audio in heavy scenes, and re-enabling audio during a link.\n\n---\n\n## 1. Core budget verdict\n\n**Honest answer to \"can we use all 4 cores\": No.** A homebrew `.cia` title gets **3 usable lanes, not 4** — 2 full cores + 1 syscore time-slice. Core 3 is OS-reserved (QTM runs at very high priority there and the N3DS GPU driver depends on it); `threadCreate(coreId=3)` returns `0xD9001BEA` on a stock kernel and even custom-kernel hacks starve. Design around 3 lanes. (`thread.h:42-46`, `svc.h:826-831`.)\n\nThe three lanes:\n- **Core 0** — full, default appcore. No prerequisite.\n- **Core 2** — full, dedicated, N3DS-only, unlocked by exheader `0x2000` = `CanAccessCore2` (already in `app.rsf`).\n- **Core 1** — syscore, **exactly ONE app thread**, unlocked by `APT_SetAppCpuTimeLimit(non-zero)` (already called, 80, `main.c:728`). It's a *time-slice shared with the OS*, not a full core — perfect for a light/bursty thread, fatal for a 16.6ms emulator.\n\n**Final thread → core → priority assignment** (`mainPrio` = 0x30, read at `main.c:741`):\n\n| Thread | Core | Priority | Role | Status |\n|---|---|---|---|---|\n| Worker A (mGBA A) | **0** | `mainPrio+1` (0x31) | runFrame game A | unchanged (`main.c:292`) |\n| Worker B (mGBA B) | **2** (N3DS) / 1 (O3DS) | `mainPrio+1` (0x31) | runFrame game B | unchanged (`main.c:293`) |\n| **Main** (input + all GPU submit + `upload_frame`×2) | **0** | `mainPrio` (0x30) | citro3d, rule-2 GPU owner | unchanged |\n| **Audio** (NEW, dedicated) | **1** (N3DS) / fallback `-1` (O3DS) | `mainPrio-1` (0x2F, numerically lower = higher) | drain SPSC rings, mix, ndsp | NEW |\n\nWhy `mainPrio-1` for audio: it must preempt main/workers briefly to refill ndsp so the channel never underruns; it blocks on the ndsp callback event the rest of the time, so it starves no one. Why not a second worker on core 1: the slice is a fraction of a syscore and OS-contended — a full GBA core would stutter; only audio (a few hundred µs/frame, mostly blocked) fits.\n\n**O3DS (best-effort, rule 1):** there is no core 2 and the core-1 slice is thinner. The `isN3DS` branch (`main.c:292-293`) already pins worker B to core 1 there. On O3DS the audio thread cannot also take core 1 (one-thread limit). Plan: on O3DS, create the audio thread with `core_id = -1` (any/affinity) — it will land on core 0 or 1 as the scheduler allows. This keeps the new path from regressing O3DS into a thread-create failure. Detect via the `isN3DS` already threaded into `run_session`.\n\n**Confirm the 80% slice is actually granted:** the analyses note the effective cap is ExHeader-dependent (5-89% legal range). Read it back at startup with `APT_GetAppCpuTimeLimit(&slice)` (`apt.h:521`); if `slice != 80`, makerom clamped it — drop to a value the exheader permits (try 70-75 if you see APT/home-menu hitches). This is also the precondition that makes the core-1 audio thread creatable at all.\n\n---\n\n## 2. Audio-offload implementation\n\n### Design: per-core lock-free SPSC handoff + one ndsp-callback-driven audio thread\n\nThe constraint that forces this design (verified in mGBA source): each core's `mAudioBuffer` is a **single non-atomic ring**, written *and* read with `buffer->size += / -=` with **no internal lock**. Today it's safe only because the per-frame handshake (`main.c:419-425`) guarantees the worker is parked on `done` before main reads. A free-running audio thread reading that ring while the worker writes it is a data race — *exactly* the \"buffer race\" the linked path dodges by muting (`main.c:414-417`).\n\n**Solution:** the worker (which already owns its core ring exclusively at well-defined points) drains its own core ring into a per-core SPSC ring; the audio thread consumes only the SPSC. Then:\n- **Core ring:** written by worker (inside runFrame/runLoop), read by worker (the drain) — *same thread, no race, even during a link.*\n- **SPSC ring:** one writer (that worker), one reader (audio thread) — safe with release/acquire atomics, no mutex on the hot path.\n\nThis is strictly better than a mutex around the core read (a mutex would re-serialize the audio thread against the worker's hot path). And because each core's ring is only ever touched by its own worker, **the linked case becomes audible** — the deferred mute is removed.\n\n### Files / functions to change\n\n**`source/audio.h` — new API (old `audio_feed` removed):**\n```c\n// Lifecycle of the dedicated audio thread (owns ndsp channel 0). Call after the\n// two cores are set up; stop before they're destroyed.\nvoid audio_thread_start(s32 mainPrio, bool isN3DS);\nvoid audio_thread_stop(void);\n\n// Worker-side handoff: the worker drains its core ring into this core's SPSC.\n// Call ONLY from that core's worker thread, right after producing a frame.\n// `slot` is 0 for emuA, 1 for emuB.\nvoid audio_pump_core(int slot, GbaCore* core);\n\n// Publish menu/focus/mode/volume params (called from main; lock-guarded).\nvoid audio_set_params(int focused, int mode, int volA, int volB);\n\n// Bind a core's SPSC->ndsp rate + request a clean cut (mode/focus/pause change).\n// These post a request the audio thread honors; main never touches ndsp directly.\nvoid audio_set_rate(GbaCore* anyCore);   // signature unchanged\nvoid audio_reset_stream(void);            // signature unchanged (now async)\n\n// audio_init / audio_exit / audio_ready: unchanged (ndspInit/ndspExit stay app-scope).\n```\n\n**`source/audio.c` — internals:**\n\n1. **Per-core SPSC ring** (×2, indexed by slot), power-of-two capacity ≈ 8192 stereo frames:\n   ```c\n   typedef struct { int16_t* buf; u32 cap; volatile u32 head, tail; } Spsc; // head=writer, tail=reader\n   ```\n   Producer (worker): `__atomic_load_n(&head, ACQUIRE)` / `__atomic_store_n(&head, ..., RELEASE)`. Consumer (audio): symmetric on `tail`. Use libctru `__atomic_*` macros from `synchronization.h`; ARM11 is weakly ordered so the release/acquire pairing is mandatory (a bare `volatile` can let the consumer read stale buffer bytes past a stale index). This is the one place to get exactly right.\n\n2. **`audio_pump_core(slot, core)`** — `gbacore_read_audio(core, tmp, n)` then SPSC-write. If the SPSC is full (audio thread fell behind), **drop oldest / skip** — never block the worker.\n\n3. **Audio thread** `audio_thread_fn` (core 1, `mainPrio-1`), owns ndsp channel 0:\n   ```\n   ndspSetCallback(audio_cb, NULL);     // fires each DSP frame (~5ms): LightEvent_Signal(&s_dspEv)\n   while (!s_quit) {\n     if (s_resetReq) { ndspChnWaveBufClear(0); audio_wbuf_reset(); spsc_clear_both_consumer_side(); s_resetReq=0; }\n     if (s_rateReq)  { ndspChnSetRate(0, s_pendRate); s_resetReq=1; s_rateReq=0; }\n     while (audio_wbuf_free()) {        // reuse existing s_wbuf ring logic\n        // read both SPSC rings, apply SOLO/MIXED/SPLIT + volA/volB\n        // (reuse the exact mix math from audio.c:101-122), then audio_queue()\n        ...\n     }\n     LightEvent_Wait(&s_dspEv);         // RESET_ONESHOT, like the worker events\n   }\n   ```\n   The mix loop, `DSP_FlushDataCache`, and `ndspChnWaveBufAdd` (the whole old `audio_feed` body) now live here, on core 1 — they leave core 0 entirely. The drift drain (`audio.c:124-125`) moves to the SPSC fill level: if a ring exceeds ~2 buffers, drop oldest, so a drifting linked core trims rather than overflows.\n\n4. **Pacing = the ndsp callback, NOT the render frame and NOT a sleep.** `ndspSetCallback` (`ndsp.h:174`, `typedef void (*ndspCallback)(void*)` at `ndsp.h:78`) fires once per DSP audio frame (~5ms); the callback does `LightEvent_Signal(&s_dspEv)`. This is the canonical libctru ogg-vorbis pattern and is *the underrun fix*: refills now happen on the DSP's 5ms cadence regardless of how long a heavy render frame takes. (The dropouts today exist because the only refill chance was once per render frame.)\n\n5. **Wave-buffer sizing:** keep `AUDIO_FRAMES=1280`, `AUDIO_DSP_BUFS=4` (= 4×~26ms ≈ 107ms buffering); the existing `s_wbuf[4]` + `s_audioMem` ring (`audio.c:12-13`) is reused as-is. Raise to 5-6 buffers only if you still hear gaps on hardware — at no latency cost that matters for an emulator.\n\n6. **Concurrency guard:** `audio_set_params` / `audio_reset_stream` / `audio_set_rate` are called from **main** (focus/mode/pause, `main.c:369,467,307`). They no longer touch ndsp; they set guarded fields/flags (`s_resetReq`, `s_rateReq`, `s_pendRate`, and the published `focused/mode/volA/volB`) under a small `LightLock` (`synchronization.h:183`), honored by the audio thread at its next wake. The audio thread is the *sole* caller of `ndspChnWaveBufAdd/Clear` after init.\n\n**`source/main.c` changes:**\n\n- **`EmuInstance`:** add `int slot;` (0/1) so the worker knows which SPSC to pump. Set in `emu_start`.\n- **`worker_main` unlinked path** (`main.c:91-93`): after `emu_step(e)`, before `LightEvent_Signal(&e->done)`, call `audio_pump_core(e->slot, e->core)`. Same single-threaded ownership window as today.\n- **`worker_main` linked path** (`main.c:85-90`): after each `gbacore_run_loop`, call `audio_pump_core(e->slot, e->core)`. The worker both produces (in runLoop) and drains — the core ring is never cross-thread, so audio survives the link.\n- **Delete the synchronous `audio_feed(...)` at `main.c:425`.** Main no longer reads any core ring (ownership moved fully to the workers/audio thread).\n- **Remove the linked mute** (`main.c:413-417`): the `linkOn` branch now does the same `LightEvent_Signal/Wait` decoupling OR keeps free-run, but audio plays either way because the workers pump their SPSC. The \"Audio is paused while linked\" comment goes.\n- **Focus/mode/pause handlers** (`main.c:369,465-467`): replace direct state with `audio_set_params(focused, audioMode, volA, volB)` + `audio_reset_stream()` (now async).\n- **`run_session` setup** (after `setup_core`, around `main.c:295-307`): `audio_set_rate(anyCore)` then `audio_thread_start(mainPrio, isN3DS)`.\n- **`run_session` teardown** (`main.c:636-648`): call `audio_thread_stop()` (sets `s_quit`, `LightEvent_Signal(&s_dspEv)` to wake it, `ndspSetCallback(NULL,NULL)`, threadJoin/threadFree) **before** `teardown_core` — so the audio thread can never read a freed SPSC. Keep `audio_init/audio_exit` at app scope in `main()` (`main.c:734,759`).\n\nThis keeps the pure-C boundary (SPSC + mix stay in `audio.c`, no mGBA/citro types) and takes 100% of audio off core 0 and off the frame path.\n\n---\n\n## 3. 804MHz / L2 detection + warning\n\n### Keep the runtime lever, don't trust the flag\n`osSetSpeedupEnable(true)` (`main.c:727`) is the **real** lever — it calls `PTMSYSM_ConfigureNew3DSCPU(3)`. Per the corrected attribution: exheader Flag1 is `bit0=L2 / bit1=804MHz`; the **PTMSYSM command's u8** is `bit0=clock / bit1=L2` (`ptmsysm.h:131-134`); either ordering, **value 3 = both on**, and NS only ever sends 0x0 or 0x3 anyway. The exheader only grants *permission*; the runtime call flips the clock. It's a safe no-op on O3DS — **call it unconditionally** (drop the `if (isN3DS)` guard at `main.c:727`; harmless and simpler).\n\n### The HUD does NOT need fixing\n`svcGetSystemTick` is **fixed at 268MHz** (`SYSCLOCK_ARM11` = 268,111,856 Hz) in *all* clock modes — it does not scale to 804. The worst-ms HUD at `main.c:631` divides by `SYSCLOCK_ARM11`, which yields correct wall-clock ms at both 268 and 804. **No N3DS correction factor.** Corollary: you **cannot** detect 804 by comparing `svcGetSystemTick` to `osGetTime` — both ride the same 268MHz counter and track 1:1.\n\n### The empirical detector (run once at startup, main thread, before workers)\nCompare *CPU work accomplished* against the fixed-rate tick: a fixed-iteration busy loop completes ~3× faster at 804 than 268, while ticks accrue at the same 268MHz rate.\n```c\nstatic u64 busy_ticks(void) {\n    volatile u32 x = 0;\n    u64 t0 = svcGetSystemTick();\n    for (volatile u32 i = 0; i < 8000000u; i++) x += i;   // pure ALU, volatile defeats optimizer\n    return svcGetSystemTick() - t0;\n}\n// Call AFTER osSetSpeedupEnable(true), on main, before run_splash/workers. Take min of 2-3 runs.\n```\n**Calibrate per build** (the absolute delta depends on the compiled loop + L2 state): print `d` on a known-268 boot and a known-804 boot, store the constant `D268`, classify fast via a *ratio*, not a hardcoded ms: `bool clockFast = (d * 173 < D268 * 100);` (804/268 = 3.0, geometric midpoint ≈1.73×). Gate the whole thing on `APT_CheckNew3DS()==true`. `APT_GetAppCpuTimeLimit(&slice)` confirms a *different* thing (the syscore slice == 80), not the clock — use it only to verify the slice.\n\n### Startup check + exact warning text\nInsert in `main()` right after `APT_SetAppCpuTimeLimit(80)` (`main.c:728`); surface the warning on the splash or a one-frame banner (the project has citro2d text already):\n```c\nbool isNew3DS=false; APT_CheckNew3DS(&isNew3DS);\nosSetSpeedupEnable(true);                  // unconditional; no-op on O3DS\nAPT_SetAppCpuTimeLimit(80);\nu32 slice=0; APT_GetAppCpuTimeLimit(&slice);     // expect 80; if not, exheader clamped\nbool clockFast = isNew3DS && detect804();\n```\nWarning copy (truthful — the only real user lever is \"run the .cia\"):\n- **Not New 3DS:** `\"Old 3DS detected. Two GBA cores need a New 3DS (804MHz + L2 + core 2). Expect heavy slowdown.\"`\n- **New 3DS but slow:** `\"New 3DS detected, but running SLOW (268MHz / no L2 / no core 2).\\nYou are likely running the .3dsx from the Homebrew Launcher.\\nInstall and run the .CIA for full speed (804MHz + L2 + core 2).\"`\n- **New 3DS + fast:** no warning (optionally assert the core-2 worker thread was actually created — `emuB.core_id == 2` after `svcGetProcessorID`, which `worker_main` already records at `main.c:75`).\n\nThere is no per-launch \"enable accelerations\" toggle beyond installing/running the `.cia`; a `.3dsx` under HBL inherits the host title's ~268MHz/no-L2/no-core-2 state and cannot reliably claim them.\n\n---\n\n## 4. Ordered, low-risk rollout\n\nEach step is independently shippable and independently measurable. Validate on **real New 3DS** (rule 6) — Azahar models neither core-1 contention, the 268-vs-804 distinction, L2, nor the ndsp callback cadence.\n\n**Step 0 — Instrument first (no behavior change, lowest risk).** Add three `svcGetSystemTick` spans to the HUD: (1) worker A run-frame time (bracket `gbacore_run_frame` in `emu_step`, store `e->lastRunTicks`), same for B; (2) the main stall = ticks across `Signal(go)…Wait(done)×2` (`main.c:419-422`); (3) the `audio_feed` cost (`main.c:425`). Run a heavy scene **Solo vs Mixed**. Expected tell that confirms the diagnosis: in Mixed, span (3) grows AND worker A's effective completion (span 2) grows by a similar amount, while worker B (core 2) is unchanged. This proves core-0 contention is the dominant cost (vs APU or GPU upload) *before* you refactor — so you know the offload will pay off. **Measure:** the Solo-vs-Mixed delta on span 2.\n\n**Step 1 — 804/L2 detector + warning (Section 3).** Pure addition, cannot regress the emulation paths. Calibrate `D268` on a 268 boot (HBL `.3dsx`) and an 804 boot (installed `.cia`). **Measure:** detector correctly says fast for the `.cia`, slow for the `.3dsx`; HUD ms is sane in both. This also *explains* any \"it's slow\" reports independent of the audio work.\n\n**Step 2 — SPSC + audio thread, UNLINKED path only (the main win).** Implement Section 2 but leave the linked mute in place for now (don't touch `main.c:413-417` yet). This isolates the highest-value change from the riskiest one. **Measure:** the heavy scene that dropped to ~30fps with audio ON now holds 60 with audio ON; worst-ms HUD shows main's per-frame work dropped by the old span-(3) amount; no audio crackle/underrun under the 80% slice. If you hear underruns: bump `AUDIO_DSP_BUFS` to 6, then if still bad raise/tune the CPU time limit (70-89 legal). If the core-1 thread won't create, fall back to `core_id=-1`.\n\n**Step 3 — Lift the link mute (the deferred item).** Now that each core's ring is worker-private and the SPSC handoff is proven in Step 2, enable `audio_pump_core` in the linked branch and delete `main.c:413-417`'s silence. **Measure:** audio plays during a link with no buffer-race crash and no drift blowup (the SPSC trim drain caps it). If a linked core drifts, confirm the trim (drop-oldest when SPSC > ~2 buffers) is firing.\n\n### Regression risks to watch\n- **Working link (v0.8):** Step 3 is the only step that touches the link path; Steps 1-2 leave it byte-identical. The link's correctness rests on the worker-cooperative `runLoop`/`wantWait` lockstep (`main.c:79-90`) — `audio_pump_core` runs *after* `run_loop` returns, inside the worker's own ownership window, so it cannot perturb the lockstep timing or the earlyExit park point. Keep the existing detach/teardown order; only *add* `audio_thread_stop()` **before** `teardown_core`.\n- **The 60fps non-audio path:** unchanged — audio off means the SPSC rings just fill and the audio thread idles on the dsp callback; main does no audio work either way.\n- **Core-1 underrun (the genuine unknown):** the syscore slice is OS-contended; if audio's burst + OS work ever exceeds it you'll hear crackle. Mitigations in priority order: deeper wavebuf (4→6), then CPU time limit tuning, then (last resort) move the audio thread to `core_id=-1`. This is the item most likely to need a hardware tuning pass — it is *not done until it runs on real New 3DS*.\n- **What this does NOT fix:** the constant APU cost inside `runFrame` (present in both audio-on and audio-off measurements). If a pathological scene makes worker A alone exceed 16.6ms even with main near-idle, you still drop frames — fall back to the existing unfocused frameskip (`main.c:343-344, 475-481`) / SOLO mode. Step 0's instrumentation tells you whether you're in that regime.\n\n### Relevant files (all absolute)\n- `/Users/guyshtainer/VSCodeProjects/3ds-toolkit/projects/3DGBA/source/audio.c` — SPSC rings, audio thread, ndsp callback, mix (move `audio_feed` body here)\n- `/Users/guyshtainer/VSCodeProjects/3ds-toolkit/projects/3DGBA/source/audio.h` — new API (Section 2)\n- `/Users/guyshtainer/VSCodeProjects/3ds-toolkit/projects/3DGBA/source/main.c` — `EmuInstance.slot`, `worker_main` pump calls (lines 85-93), delete `audio_feed` (425), lift link mute (413-417), audio thread start/stop in `run_session` (295-307, 636-648), `main()` detector+warning (724-728)\n- `/Users/guyshtainer/VSCodeProjects/3ds-toolkit/projects/3DGBA/source/gbacore.{c,h}` — no changes needed; the `gbacore_audio_available/read/drain` helpers (`gbacore.c:167-177`) are already the SPSC producer's source\n\nNo build-system change: the Makefile auto-globs `source/`, and no new `.c` files are added.",
    "findings": [
      {
        "key": "core-model",
        "research": {
          "report": "## The definitive New-3DS CPU core model for an application TITLE (.cia)\n\n### `threadCreate(coreId)` / `svcCreateThread(processorid)` — what each value does\n\nThe authoritative source is the libctru `thread.h` doc block, which is a verbatim restatement of 3dbrew's Multi-threading page. From `/opt/devkitpro/libctru/include/3ds/thread.h:36-46`:\n\n> - `core_id` ... On Old3DS it must be `<2`, and on New3DS it must be `<4`. Pass `-1` to execute the thread on all CPUs and `-2` to execute the thread on the default CPU (read from the Exheader).\n> - Processor #0 is the application core. It is always possible to create a thread on this core.\n> - Processor #1 is the system core. If `APT_SetAppCpuTimeLimit` is used, it is possible to create a single thread on this core.\n> - Processor #2 is New3DS exclusive. Normal applications can create threads on this core if the exheader kernel flags bitmask has `0x2000` set.\n> - Processor #3 is New3DS exclusive. **Normal applications cannot create threads on this core.**\n> - Processes in the BASE memory region can always create threads on processors #2 and #3.\n\n`svcCreateThread` (`svc.h:826-831`) restates: \"For old 3ds it has to be `<2`, and for new 3DS `<4`. ... The processor with ID 1 is the system processor. To enable multi-threading on this core you need to call `APT_SetAppCpuTimeLimit` at least once with a non-zero value.\"\n\n### Core 0 — appcore (default)\nAlways available; the main thread runs here by default. `threadCreate(..., core_id=0, ...)` always succeeds. No prerequisites. (`thread.h:42`)\n\n### Core 1 — syscore (the OS / system thread core)\n- An application **can** put threads here, but **only after** `APT_SetAppCpuTimeLimit(percent)` with a non-zero percent. (`thread.h:43`, `svc.h:830-831`)\n- 3dbrew APT:SetApplicationCpuTimeLimit: \"This sets the amount of syscore CPU time available to the running application. ... It can range from **5% to 89%**. **Maximum value depends on the ExHeader.**\" This is corroborated by libctru `exheader.h:150`: the reslimit array \"only 'CpuTime' (first byte) seems to be used\" — i.e. the exheader's CpuTime descriptor is the hard cap. `RESLIMIT_CPUTIME = 9` (`svc.h:127`) is the underlying kernel resource limit (\"CPU time. Value expressed in percentage\").\n- **Key constraint from `thread.h:43`: only ONE thread can be created on core 1** (\"it is possible to create a *single* thread on this core\"). You do not get a free full core — you get a *time slice* carved out of the syscore, time-shared with the OS's own system threads (APT, the home menu manager, sound/csnd helpers, etc.).\n- This slice **exists on BOTH Old and New 3DS** — it is the original \"second core\" of the Old3DS and is the *only* way an Old3DS app gets parallelism. 3dbrew: \"One core is always dedicated to the OS, hence you will never get 100% of both cores.\"\n\n### Core 2 — New-3DS only, full core, gated by exheader `0x2000`\n- New-3DS exclusive. A normal (non-BASE) application gets it **only if exheader ARM11-kernel-flags bitmask `0x2000` is set** — this is exactly the `CanAccessCore2` flag the project's `app.rsf` already grants. Otherwise `svcCreateThread` returns `0xD9001BEA`. (`thread.h:44`; 3dbrew Multi-threading / SVC CreateThread.)\n- This is a **true, full, dedicated core** (not a time-slice like core 1). No `APT_SetAppCpuTimeLimit` needed for it. The project already targets and uses this correctly for Worker B. **Confirmed.**\n\n### Core 3 — New-3DS 4th core — OS-RESERVED, NOT usable by applications\nDefinitive: **No, a normal application title cannot use core 3, full stop.**\n- `thread.h:45`: \"Processor #3 is New3DS exclusive. **Normal applications cannot create threads on this core.**\"\n- Kernel-level: `svcCreateThread` with `processorid==0x3` for a non-BASE-mem-region process returns error `0xD9001BEA` (3dbrew Multi-threading / SVC). The `0x2000` exheader flag does NOT unlock core 3 — it only unlocks core 2.\n- The *why*, straight from the Luma3DS kernel maintainer (TuxSH, LumaTeam/Luma3DS PR #1668, which exists precisely because stock kernel blocks core 3): **\"QTM is running at very high priority on core3, and the GPU driver depends on QTM on N3DS. User applications are not meant to use core3 at all.\"** Even when a *custom kernel patch* force-unlocks core 3, apps suffer thread starvation because QTM (the New-3DS head-tracking / 3D-depth service the GPU driver depends on) runs at very high priority there. TuxSH's explicit advice: **\"Just use core2 instead.\"**\n- Only processes in the BASE memory region (system modules, not user .cia titles) can ever touch core 3. A homebrew `.cia` is not BASE-region.\n\n### Bottom line — usable execution lanes on New 3DS for this title\nYou have **three** independent execution lanes, not four:\n1. **Core 0** — full core, your main/render/appcore thread (default).\n2. **Core 2** — full dedicated core (you have `CanAccessCore2`). Heavy worker OK.\n3. **Core 1** — a *time-shared slice* of the syscore, ONE thread only, unlocked by `APT_SetAppCpuTimeLimit`. Shared with the OS — good for a LIGHT thread, risky for a full-speed emulator.\n\nCore 3 is hardware-present but **OS-reserved (QTM); unreachable from a user title**. \"Use all 4 cores\" is not achievable for a homebrew .cia — the honest ceiling is 2 full cores + 1 syscore slice.\n\n### `coreId` values that actually work for `threadCreate` in this title\n- `0` → always works (appcore).\n- `2` → works (you set `CanAccessCore2`/`0x2000`).\n- `1` → works for exactly ONE thread, only after `APT_SetAppCpuTimeLimit(non-zero)`.\n- `3` → fails with `0xD9001BEA`. Do not use.\n- `-2` → default-from-exheader (the project's `ideal_processor`/`affinity_mask` in `exheader.h:139-140`); `-1` → all-CPUs affinity.",
          "recommendation": "Treat New 3DS as **2 full cores + 1 syscore time-slice = 3 usable lanes**, NOT 4. Concretely for 3DGBA:\n\n1. Keep the current pinning of the two HEAVY mGBA workers on **core 0** and **core 2** (the two full cores). These are the only two lanes that can sustain a full-speed emulator.\n2. To offload the audio mixing that is costing you frames, move `audio_feed()` (mix + `ndspChnWaveBufAdd`) into a **dedicated LIGHT thread pinned to core 1 (the syscore)** — call `threadCreate(audio_thread, ..., core_id=1, detached=false)`. Core 1 is the right home for audio precisely because audio mixing is light and bursty, so it tolerates time-sharing with the OS, whereas a full-speed emulator on core 1 would stutter under OS contention.\n3. Prerequisite (already partly done): you call `APT_SetAppCpuTimeLimit(80)` — that is in the valid 5–89% range and is exactly what unlocks the single allowed core-1 thread. Note the *effective* cap is \"Maximum value depends on the ExHeader\" CpuTime descriptor, so confirm your app.rsf's CpuTime/SystemControlInfo allows 80; if makerom rejects/clamps it, drop to a value the exheader permits. 80% is aggressive but legal; if you see APT/home-menu hitches, try 70–75%.\n4. Core 1 allows only ONE app thread — spend it on audio. Do not also try to put a second worker there.\n5. Do NOT attempt core 3 (coreId=3). It returns 0xD9001BEA on a stock kernel; it is QTM-reserved and even custom-kernel hacks starve there. Designing around \"4 cores\" is a dead end — design around 3 lanes.\n\nThis directly addresses the measured symptom: with audio on its own core-1 thread, the per-frame main thread no longer blocks on mix/`ndspChnWaveBufAdd` during heavy scenes, which is what was dragging hard scenes from 60 to 30 fps. (Validate on real New 3DS per the project's hardware-final gate — Azahar does not model core-2/core-1 contention faithfully.)",
          "confidence": "HIGH",
          "risks": "- The core-count verdict (0/1/2 usable, 3 reserved) is rock-solid: libctru thread.h, svc.h, 3dbrew Multi-threading, and the Luma3DS maintainer all agree, including the exact 0xD9001BEA error and the QTM-on-core3 reason. Very low risk of being wrong here.\n- The 89% cap and \"depends on ExHeader\" come from 3dbrew (via WebFetch summary, not a byte-exact quote of the table) and are corroborated by exheader.h:150 (CpuTime is the only reslimit byte used) and RESLIMIT_CPUTIME=9 \"expressed in percentage.\" If a byte-exact citation is required, read the raw 3dbrew APT:SetApplicationCpuTimeLimit page directly; the numeric bound could differ by a point or two but the mechanism is certain.\n- \"Only ONE thread on core 1\" is stated by libctru thread.h and is reliable, but if you ever need more concurrency on syscore you cannot get it from a user title — plan accordingly.\n- Performance CLAIM (that moving audio to core 1 fixes the 30fps drop) is an engineering recommendation, NOT yet measured. The core MODEL supports it, but per CLAUDE.md's \"Done gate\" it is unproven until it runs on real New 3DS — Azahar will not faithfully reproduce core-1 OS contention, so the win must be confirmed on hardware. There is a real chance core-1 OS contention (APT/home-menu/csnd) introduces audio underruns under an 80% limit; you may need to tune the percent or the wavebuf depth.\n- This analysis is the CPU-core dimension only. It does not by itself prove the GPU-upload or ndsp-config paths aren't also contributing; another dimension should confirm whether ndspChnWaveBufAdd/mix is truly the dominant cost vs. the linear→tiled DisplayTransfer."
        },
        "verify": {
          "verdict": "CONFIRMED",
          "corrections": "Minor only; verdict unchanged. See notes for the six precision fixes.",
          "notes": "All key hardware claims re-verified against the headers under /opt/devkitpro/libctru/include and against 3dbrew plus Luma3DS. CORE MODEL CONFIRMED: thread.h lines 42 to 46 and svc.h lines 826 to 831 match the analysis verbatim, namely core 0 always available, core 1 a single thread only after APT_SetAppCpuTimeLimit, core 2 needs exheader kernel flag 0x2000, core 3 forbidden for non-BASE processes. 3dbrew Multi-threading confirms error 0xD9001BEA for both core 2 without 0x2000 and core 3 from a non-BASE process. Luma3DS PR 1668 context confirms QTM runs at very high priority on core 3 and the GPU driver depends on it, so user applications must not use core 3 and should use core 2 instead. Usable lanes therefore equal three: two full cores 0 and 2, plus one single-thread syscore slice on core 1; using all four cores is impossible for a homebrew title.\n\nSIX PRECISION FIXES (none change the verdict): 1, svc.h line 127 full text is CPU time Value expressed in percentage regular until it reaches 90; the analysis truncated it after percentage. 2, the 5 to 89 percent range and ExHeader-dependent maximum are byte-confirmed on 3dbrew APT SetApplicationCpuTimeLimit. 3, the 0x2000 bit 13 is 3dbrew Process has access to CPU core 2; CanAccessCore2 is the same bit RSF name, not a separate flag. 4, svcGetSystemTick does NOT scale with the 804MHz boost: os.h lines 16 to 17 fix it to SYSCLOCK_ARM11 equal to 268,111,856 Hz, so tick deltas measure wall-time not CPU cycles, and at 804MHz you get about 3x more CPU work per tick. 5, osSetSpeedupEnable is setter-only at os.h line 331 and internally drives PTMSYSM ConfigureNew3DSCPU; for an installed cia title the boost requires BOTH the RSF rights CpuSpeed 804MHz plus EnableL2Cache plus a non-Legacy N3DS SystemModeExt AND the runtime call osSetSpeedupEnable true, and the project does both. 6, SYSCLOCK_ARM11_NEW which is LGR2 equals 804,335,568 Hz about 804MHz.\n\nPROJECT GROUNDED: in projects/3DGBA/source/main.c worker A is pinned to core 0 at line 292, worker B to core 2 on N3DS else core 1 at line 293, and APT_CheckNew3DS plus osSetSpeedupEnable true plus APT_SetAppCpuTimeLimit 80 are at lines 726 to 728; app.rsf sets CpuSpeed 804MHz, EnableL2Cache true, CanAccessCore2 true, MemoryType Application. The recommendation to move audio_feed, the mix plus ndspChnWaveBufAdd, to a dedicated light thread pinned to core 1 is architecturally sound given the verified model, since core 1 is the only remaining lane and suits light bursty audio rather than a full-speed emulator, and the existing APT_SetAppCpuTimeLimit 80 is exactly the prerequisite.\n\nUNPROVEN per the project Done-gate: the 60 to 30 fps fix is an engineering hypothesis not measured on real hardware, and Azahar does not model core contention faithfully; core 1 OS contention from APT, home menu, or csnd could cause audio underruns at an 80 percent limit, in which case tune to 70 to 75 percent or adjust wavebuf depth; the CPU-core dimension alone does not prove that mix or ndspChnWaveBufAdd is the dominant cost versus the linear to tiled GX_DisplayTransfer, so a second GPU or audio-config check is warranted; and 80 percent must be within the exheader CpuTime descriptor at exheader.h line 150 or makerom may clamp it."
        }
      },
      {
        "key": "optimal-pinning",
        "research": {
          "report": "## Diagnosis: why \"audio ON\" halves FPS\n\nI read the actual code. The frame loop is in `run_session()` at `projects/3DGBA/source/main.c:346-634`. The unlinked hot path is `main.c:418-426`:\n\n```\nLightEvent_Signal(&emuA.go);   // wake worker A (core 0)\nLightEvent_Signal(&emuB.go);   // wake worker B (core 2)\nLightEvent_Wait(&emuA.done);   // main BLOCKS until both\nLightEvent_Wait(&emuB.done);\nif (emuA.core) upload_frame(&emuA);   // GSPGPU flush + C3D_SyncDisplayTransfer\nif (emuB.core) upload_frame(&emuB);\naudio_feed(...);               // mix + DSP_FlushDataCache + ndspChnWaveBufAdd\n```\n\nWorker A is pinned to **core 0**, the same core the **main thread** runs on (`emu_start(&emuA, 0, 0, ...)` at `main.c:292`; main is the default appcore = core 0). Worker B is alone on **core 2** (`main.c:293`). So core 0 is shared (main + worker A) while core 2 has worker B to itself. This asymmetry is the crux.\n\nThere are three candidate audio costs. Evaluating each against the code:\n\n**(a) Main-thread mix/feed CPU on core 0 starving worker A — MOST LIKELY.**\n`audio_feed` (`audio.c:76-126`) runs **synchronously on the main thread** every frame, *after* `LightEvent_Wait(done)`. In MIXED/SPLIT it loops `while (audio_wbuf_free())`, and for each ~549-frame chunk it reads *both* cores (`gbacore_read_audio` x2 = circle-buffer memcpy), does a per-sample mix loop (`audio.c:110-120`), then `audio_queue` does `DSP_FlushDataCache` (`audio.c:71`) — an L1/L2 clean over up to 1280*2*2 = ~5 KB and an **ndsp service IPC call** (`ndspChnWaveBufAdd`). Crucially this serializes against worker A: main + worker A timeshare one core, so every microsecond main spends in `audio_feed`/`upload_frame` is a microsecond worker A does *not* get. In a heavy scene worker A is already near its ~16.6 ms budget; adding the audio + 2 display transfers + 2 GSPGPU flushes onto the *same core* pushes the slowest worker past one refresh, and `C3D_FRAME_SYNCDRAW` (`main.c:575`) then parks on vblank → the classic 60→30 halving. Worker B on core 2 is fine; the bottleneck is core 0 contention. The symptom (audio OFF = solid 60, audio ON = 30 in heavy scenes only) fits this exactly: with audio off, core 0 = main(light) + worker A(heavy) just fits; with audio on it tips over the vblank edge and SYNCDRAW doubles the period.\n\n**(b) APU emulation inside both mGBA `runFrame` — REAL but NOT toggled by your audio path.** The expensive APU work is `GBAAudioSample`/`_sample` (`src/gba/audio.c:256+`, scheduled at the sample interval, doing per-sample PSG+FIFO mix + `_applyBias`) and FIFO DMA. But this runs inside `gbacore_run_frame` **regardless** of whether you call `audio_feed` — the core always produces samples into its ring (`gbacore.c:143-147`, buffer size 4096 at `gbacore.c:82`). So the APU cost is present in *both* your measurements and cannot explain the difference between audio-ON and audio-OFF. (It does explain why both cores are heavy in the first place, i.e. why there's no slack to absorb (a).) Note: `mAudioBufferRead/Available/Clear` (`src/util/audio-buffer.c:47,19,27`) are pure circle-buffer ops — **no resampling on read** — so the read side is cheap; the cost is the flush + IPC + the mix loop, all on main.\n\n**(c) ndsp cache flush / DSP contention — CONTRIBUTING, second-order.** `DSP_FlushDataCache` (`dsp.h:78`) plus `ndspChnWaveBufAdd` are an IPC round-trip to the dsp service + a cache clean; multiple per frame when the ring has several free buffers. This is real cost but it lands **on core 0 inside `audio_feed`**, so it folds into (a). It is not separately fatal.\n\n**Verdict: (a) is dominant** — the audio work is CPU on core 0 that competes with worker A, inside the frame-critical path gated by SYNCDRAW. (c) is part of (a). (b) is the reason there's no headroom but is constant across both measurements.\n\n### How to confirm with svcGetSystemTick (`svc.h:1149`, clock `SYSCLOCK_ARM11` = `os.h:17`)\nYou already wrap the whole frame at `main.c:347,631`. Add three more spans and print them in the HUD:\n1. **Worker A run-frame time**: bracket `gbacore_run_frame` in `emu_step` (`main.c:64-71`) with `svcGetSystemTick()`; store to `e->lastRunTicks`. Same for worker B. This tells you each worker's real per-core cost.\n2. **Stall = main waiting on workers**: ticks from just before `LightEvent_Signal(go)` to just after the second `LightEvent_Wait(done)` (`main.c:419-422`). Compare to max(workerA,workerB) run time. If the *wait* span is much larger than worker B's run time but ~= worker A's run time, that proves worker A (sharing core 0 with main) is the long pole.\n3. **audio_feed cost**: bracket the `audio_feed` call (`main.c:425`). \nRun the same heavy scene with audio Solo vs Mixed. The tell: in Mixed, span (3) grows AND worker A's effective completion (span 2) grows by a similar amount, while worker B (core 2) is unchanged — confirming core-0 contention, not APU and not core 2. Convert ticks to ms with `ticks * 1000.0 / SYSCLOCK_ARM11` (the file already does this at `main.c:631`).\n\n## Recommended thread→core layout\n\nKeep both heavy emulator workers on the two **full** cores, get the main thread off worker A's back as much as possible, and move audio off the frame-critical path onto the **core-1 syscore slice** (which `APT_SetAppCpuTimeLimit(80)` at `main.c:728` already unlocks — `svc.h:830-831`, `thread.h:44`). Audio is light and bursty, a perfect fit for the limited syscore budget; a heavy emulator worker is NOT (the slice is a fraction of a core and shared with the OS, so worker A there would stall).\n\n| Thread | Core | Priority | Role |\n|---|---|---|---|\n| Worker A (mGBA A) | **0** | `mainPrio+1` (0x31) | runFrame for game A |\n| Worker B (mGBA B) | **2** | `mainPrio+1` (0x31) | runFrame for game B |\n| **Main** (input + GPU submit) | **0** | `mainPrio` (0x30) | citro3d, upload_frame x2, input |\n| **Audio** (new, dedicated) | **1** (syscore slice) | `mainPrio-1` (0x2F, slightly higher) | pulls PCM from both rings, mixes, ndspChnWaveBufAdd |\n\nWhy this split:\n- **Workers stay on full cores 0 & 2.** Moving worker A to the core-1 slice (\"worker A → core 1\") is wrong: the slice is only ~the AppCpuTimeLimit fraction of one syscore and is contended by system threads; a 16.6 ms-budget GBA core cannot live there. Reject it.\n- **Audio → core 1 slice** is the right move. The whole `audio_feed` body — the mix loop, `DSP_FlushDataCache`, and the `ndspChnWaveBufAdd` IPC — leaves core 0 entirely, so it no longer steals time from worker A. That directly removes the cost that tips the heavy scene over the vblank edge. Audio's duty cycle is tiny (a few hundred µs/frame), well within the syscore slice.\n- **Audio thread priority `mainPrio-1` (numerically lower = higher).** ndsp must not underrun; giving it slightly-higher priority than main/workers ensures it preempts to refill the ring promptly. It blocks most of the time, so it won't starve anyone.\n- **Main stays on core 0 at `mainPrio`.** It still does the 2 display transfers + GSPGPU flushes (those MUST stay on the GPU-owning main thread per project rule 2). Those are cheaper than audio and unavoidable on main; with audio gone, core 0 = main(GPU submit, light) + worker A(heavy) again fits in one refresh.\n\n### Decoupling audio from the per-frame critical path\nDon't just move `audio_feed` to another thread and still join it each frame — that re-serializes. Make audio a **free-running producer/consumer** off the worker rings:\n- The audio thread loops: wait on a `LightEvent` (you already use these — `synchronization.h:281`) signalled once per frame by main right after `LightEvent_Wait(done)` (cores' rings are freshly filled then), or simpler, loop on `LightEvent_WaitTimeout` ~4-5 ms (`synchronization.h:320`); each wake, run the existing `audio_feed` body draining both rings into ndsp.\n- Guard the shared audio state (`s_wbuf`, `s_bufId`, `s_audioMem` in `audio.c:12-17`) with a `LightLock` (`synchronization.h:183`) since main no longer calls `audio_feed` but still calls `audio_reset_stream`/`audio_set_rate` on focus/mode changes (`main.c:369,467`). The mGBA ring itself is single-producer (the worker) / single-consumer (audio thread) per core — that's already safe as long as ONLY the audio thread reads a given core's ring (today main does; move that ownership to the audio thread).\n- Keep `upload_frame` (the 2 display transfers) on main between `C3D_FrameBegin/End` — unchanged, rule 2.\n- The MUTED-while-linked path (`main.c:413-417`) can now actually play: the free-running audio thread already matches the workers' free-run model, removing the original \"buffer race\" reason for muting. Use the existing per-core drift drain (`audio.c:124-125`) to keep them aligned.\n\n### Expected effect\n- Heavy scenes: the ~few-hundred-µs of audio mix+flush+IPC per frame stops competing with worker A on core 0. That is precisely the slice that was pushing the slow worker past 16.6 ms, so SYNCDRAW should hold at one refresh → **back to a solid 60 fps with audio ON** in the scenes that previously dropped to 30. Worker B (core 2) was never the problem and is untouched.\n- If a few pathological scenes still tip over, the second lever (no new threads) is the existing unfocused **frameskip** (`main.c:343-344, 475-481`) and SOLO audio mode (cheapest mix path, `audio.c:80-98`). But with audio off the critical path, those should rarely be needed.\n- Do NOT expect help from core 3 — `thread.h:45` and `svc.h:826` confirm normal apps cannot use processor #3; cores 0, 2 (full) + the core-1 slice are your entire budget, and this layout uses all three.",
          "recommendation": "Move the per-frame audio work off the frame-critical path and off core 0. Concretely:\n\nPINNING (4 threads, all the budget you have — core 3 is unusable per thread.h:45):\n- Worker A (mGBA game A): core 0, prio mainPrio+1 (0x31) — unchanged.\n- Worker B (mGBA game B): core 2, prio mainPrio+1 (0x31) — unchanged.\n- Main (input + all citro3d/GPU submit incl. upload_frame x2): core 0, prio mainPrio (0x30) — unchanged.\n- NEW Audio thread: core 1 (the syscore slice that APT_SetAppCpuTimeLimit(80) already unlocks), prio mainPrio-1 (0x2F, slightly higher so ndsp never underruns).\n\nCreate it with threadCreate(audio_thread, NULL, 32*1024, mainPrio-1, 1, false) (thread.h:51); if core 1 create fails, fall back to -2.\n\nDECOUPLE: delete the synchronous audio_feed call at main.c:425. The audio thread free-runs (LightEvent_WaitTimeout ~4ms loop, or a per-frame LightEvent signalled by main after LightEvent_Wait(done)) and runs the existing audio_feed body, draining BOTH cores' rings into ndsp. Move ring-read ownership entirely to the audio thread (main must stop reading the rings). Guard s_wbuf/s_bufId/s_audioMem (audio.c:12-17) and the reset/set_rate calls (main.c:369,467) with a LightLock (synchronization.h:183). Keep upload_frame on main between C3D_FrameBegin/End (rule 2).\n\nDo NOT put a heavy emulator worker on the core-1 slice — it's only a fraction of a syscore and OS-contended; audio (a few hundred µs/frame, mostly blocked) fits, a 16.6ms GBA core does not.\n\nExpected result: the audio mix + DSP_FlushDataCache + ndspChnWaveBufAdd IPC stop stealing core-0 time from worker A, which is what tipped heavy scenes past the 16.6ms vblank under C3D_FRAME_SYNCDRAW. FPS should hold at 60 with audio ON in the scenes that previously dropped to 30. As a bonus, the linked-mode audio mute (main.c:413-417) can be lifted since the free-running audio thread matches the workers' free-run model.",
          "confidence": "HIGH",
          "risks": "- The core-1 syscore slice is a FRACTION of a core and shared with OS threads; it is gated by APT_SetAppCpuTimeLimit (currently 80, main.c:728). If audio's burst plus OS work ever exceeds the slice, ndsp could underrun (crackle). Mitigation: audio is light and mostly blocked; if needed, raise the time limit or shrink AUDIO_FRAMES. Validate the actual slice cost with svcGetSystemTick on hardware — Azahar does NOT model core-1 slice contention or the 804MHz/L2 budget (project rule 6: not done until it runs on real New 3DS).\n- Concurrency: moving ring-read ownership to the audio thread means audio_reset_stream/audio_set_rate (called from main on focus/mode/pause changes) now race the audio thread. Must add the LightLock; otherwise a reset mid-read corrupts s_bufId/wave buffers. The mGBA per-core ring stays single-producer(worker)/single-consumer(audio) — safe only if main no longer touches it.\n- This fixes the core-0 contention (cause a). It does NOT reduce the APU emulation cost inside runFrame (cause b) — that's constant. If a scene is so heavy that worker A alone exceeds 16.6ms even with main near-idle, you still drop frames; fall back to unfocused frameskip (main.c:343) / SOLO audio.\n- Old 3DS (best-effort per rule 1) has no core 2 and a thinner core-1 slice; this 4-thread layout is New-3DS-only. The isN3DS branch (main.c:293) must keep its old behavior there.\n- I did not build/run this; the FPS recovery estimate is reasoned from the code structure + the reported symptom, not measured. Confirm with the svcGetSystemTick instrumentation described before declaring done."
        },
        "verify": {
          "verdict": "CONFIRMED",
          "corrections": "No load-bearing claim required correction. Every costly hardware claim holds against both the local libctru headers and 3dbrew. Only trivial, non-load-bearing line-number nits below (the analysis's own conclusions are unaffected).\n\n1) CORE 3 UNUSABLE BY NORMAL APPS — CONFIRMED.\n   - libctru thread.h:45: \"Processor #3 is New3DS exclusive. Normal applications cannot create threads on this core.\" (svc.h:826-828 corroborates: New3DS processor_id must be <4.)\n   - 3dbrew (Multi-threading): \"For processorid==0x3, non-BASE mem-region processes receive an error regardless.\"\n   - Verdict: the layout correctly refuses to use core 3. Cores 0+2 (full) plus the core-1 syscore slice are the entire budget for a normal .cia app.\n\n2) HEAVY THREAD ON CORE 1 — CONFIRMED the analysis's REJECTION of putting a heavy emulator worker there, and CONFIRMED that a single light (audio) thread is allowed.\n   - thread.h:43 + svc.h:830-831: core 1 is the system core; \"If APT_SetAppCpuTimeLimit is used, it is possible to create a SINGLE thread on this core.\" The word \"single\" is in the header verbatim — so the layout's one audio thread on core 1 is exactly the maximum allowed, and adding any second core-1 thread later would fail.\n   - 3dbrew confirms core 1 is the system core requiring APT:SetApplicationCpuTimeLimit.\n   - The code already calls APT_SetAppCpuTimeLimit(80) (main.c:728), so core-1 thread creation is unlocked. The reasoning that the slice is a fraction of a syscore (only the granted % of one core, OS-contended) and therefore unfit for a 16.6ms GBA worker but fit for bursty audio is sound. CONFIRMED.\n\n3) CORE 2 USE IS EXHEADER-GATED (0x2000) — CONFIRMED.\n   - thread.h:44: \"Normal applications can create threads on this core if the exheader kernel flags bitmask has 0x2000 set.\"\n   - 3dbrew (Multi-threading): processorid==0x2 for non-BASE processes requires exheader kernel-flags bitmask 0x2000.\n   - The project's app.rsf grants CanAccessCore2 (= the 0x2000 flag), so worker B on core 2 is legitimate on New3DS. CONFIRMED.\n\n4) osSetSpeedupEnable IS SETTER-ONLY AND 804MHz IS EXHEADER/RSF-GATED — CONFIRMED.\n   - os.h:331: void osSetSpeedupEnable(bool enable); — no getter exists in os.h (confirmed by grep). The \"SETTER-ONLY\" fact stands.\n   - 3dbrew (Hardware): New3DS 804MHz is gated by application flags (NCCH/Extended_Header / PDN registers). Matches the code guarding it behind APT_CheckNew3DS (main.c:726-727) and the .cia app.rsf granting 804MHz/L2. CONFIRMED.\n\n5) svcGetSystemTick CLOCK-SCALING — CONFIRMED, and this is the claim most likely to bite and the analysis got it RIGHT.\n   - libctru os.h:16: SYSCLOCK_ARM11 is documented as \"The clock rate for the Arm11 in CTR mode AND IN svcGetSystemTick\" = SYSCLOCK_ARM9*2 = 268,111,856 Hz. os.h:19-23 separately defines LGR1 (2x) and LGR2 (3x ≈ 804MHz) as the ACTUAL ARM11 clock under speedup — but svcGetSystemTick is tied to the base 268MHz, not the scaled clock.\n   - libctru's own CPU_TICKS_PER_MSEC (os.h:25) and osTickCounterRead (os.h:287) both use the fixed SYSCLOCK_ARM11.\n   - 3dbrew (Hardware + Multi-threading): \"svcGetSystemTick uses a tick counter running at 268MHz\" even when the CPU runs at 804MHz; the tick reference does NOT scale with the clock multiplier.\n   - CONSEQUENCE: the analysis's conversion `ticks * 1000.0 / SYSCLOCK_ARM11` (used at main.c:631) yields correct WALL-CLOCK milliseconds regardless of osSetSpeedupEnable. This is the right way to measure the frame/worker/audio spans for the proposed instrumentation. The tick count of WORK DONE will, of course, be fewer ticks at 804MHz than at 268MHz for the same wall-clock window (because more instructions retire per tick), but elapsed-time math is unaffected. CONFIRMED.\n\nTRIVIAL (non-load-bearing) nits, no correction needed:\n   - The analysis cites \"synchronization.h:281\" for LightEvent generally and \":320\" for LightEvent_WaitTimeout. Line 281 is actually LightEvent_Init; WaitTimeout is at 320 (correct). LightLock_Init at 183, LightLock_Lock at 189 — correct. All cited primitives exist.\n   - audio.c citations all verified exactly: s_wbuf/s_bufId/s_audioMem at 12-14 (s_mixA/s_mixB at 16-17), DSP_FlushDataCache + ndspChnWaveBufAdd in audio_queue at 71-72, mix loop 110-120, drift drain 124-125, AUDIO_FRAMES=1280 (line 7, not the \"~549 frames/GBA-frame\" which is gbacore.c:81's separate comment — the analysis correctly distinguishes these).\n   - main.c citations verified: frame loop 346-426 (analysis said 346-634, which spans the whole while; the hot path 418-426 is exact), signal/wait/upload/audio_feed at 419-425, C3D_FRAME_SYNCDRAW at 575, upload_frame = GSPGPU_FlushDataCache + C3D_SyncDisplayTransfer at 149-151, emu_step at 64-71, APT_SetAppCpuTimeLimit(80) at 728, emu_start pinning at 292-293 (A->core0, B->core2 on N3DS else core1), frameskip at 343-344/475-481, linked-mute at 413-417. All correct.\n   - gbacore.c: setAudioBufferSize(core,4096) at line 82; mAudioBufferRead/Available/Clear are pure circle-buffer ops (167-176) with no resampling on read — confirmed.",
          "notes": "SCOPE: I verified the HARDWARE/PLATFORM claims (the four flagged risks + the tick clock) and the source-code citations the analysis rests on. I did NOT build or run anything, and I did NOT independently re-measure the 60->30 FPS symptom or prove the proposed 4-thread layout actually recovers 60fps — that requires real New 3DS hardware (project rule 6; Azahar does not model core-2 contention, the core-1 slice, or the 804MHz/L2 budget). The analysis itself states this caveat.\n\nKEY HARDWARE FACTS (all CONFIRMED, with citations):\n- /opt/devkitpro/libctru/include/3ds/thread.h:42-46 — the authoritative per-core access rules: core0 always; core1 single thread iff APT_SetAppCpuTimeLimit; core2 N3DS iff exheader 0x2000; core3 N3DS forbidden for normal apps; BASE-region processes exempt for 2&3.\n- /opt/devkitpro/libctru/include/3ds/svc.h:826-831 (svcCreateThread processor rules) and :825 (priority range [0x18;0x3F]).\n- /opt/devkitpro/libctru/include/3ds/os.h:16-23 (SYSCLOCK_ARM11=268111856 is the svcGetSystemTick rate; LGR1/LGR2 are the scaled CPU clocks), :25 (CPU_TICKS_PER_MSEC), :331 (osSetSpeedupEnable, no getter).\n- 3dbrew: https://www.3dbrew.org/wiki/Multi-threading (core access + 0x2000), https://3dbrew.org/wiki/Hardware (268MHz vs 804MHz gating + fixed-268MHz tick).\n\nCODE LOCATIONS the analysis depends on (all verified):\n- /Users/guyshtainer/VSCodeProjects/3ds-toolkit/projects/3DGBA/source/main.c — frame loop 346-426, hot path 418-425, SYNCDRAW 575, upload_frame 149-151, emu_step 64-71, pinning 292-293, time-limit 728, speedup 726-727, frameskip 343-344/475-481, linked-mute 413-417, tick->ms 631.\n- /Users/guyshtainer/VSCodeProjects/3ds-toolkit/projects/3DGBA/source/audio.c — audio_feed 76-126, audio_queue (flush+IPC) 69-74, shared state 12-17.\n- /Users/guyshtainer/VSCodeProjects/3ds-toolkit/projects/3DGBA/source/gbacore.c — audio buffer 4096 at 82, circle-buffer reads 167-176.\n\nONE THING WORTH FLAGGING (does not change the verdict, but the implementer should heed it): thread.h:43 says core 1 allows exactly ONE thread when APT_SetAppCpuTimeLimit is set. The recommended layout uses that single slot for the audio thread — fine — but it leaves NO room for any future second core-1 thread, and the audio thread's threadCreate(core 1) can still FAIL at runtime (e.g. on Old3DS, or if the time-limit call was skipped). The analysis already prescribes a fallback to -2 (default appcore) on failure, which is the correct mitigation; keep it. Also note that on Old3DS there is no core 2, so the whole 4-thread split is New3DS-only and the isN3DS branch (main.c:293) must retain its current behavior — the analysis states this too.\n\nBOTTOM LINE: the diagnosis (core-0 contention from synchronous audio_feed under C3D_FRAME_SYNCDRAW is the dominant cost) is well-supported by the code structure, and the recommended pinning (workers on full cores 0+2, audio on the core-1 syscore slice, core 3 left unused) is consistent with every platform constraint I could verify. The instrumentation plan (svcGetSystemTick spans converted via /SYSCLOCK_ARM11) is the correct, clock-scaling-safe way to measure it on hardware before declaring done."
        }
      },
      {
        "key": "audio-offload",
        "research": {
          "report": "## What the code actually shows (the constraint that drives the design)\n\n**The mGBA audio buffer is a single non-atomic ring, written on the worker thread.**\n- Each core's audio sits in a `struct mAudioBuffer` wrapping a `struct mCircleBuffer` (`external/mgba/include/mgba-util/audio-buffer.h:15-18`, `circle-buffer.h:13-19`).\n- The **producer** runs on the *worker* thread: `runFrame`/`runLoop` → mGBA's `_sample` timing callback → `mAudioBufferWrite(&audio->psg.buffer, ...)` (`external/mgba/src/gba/audio.c:401-410`). So audio is generated *inside* `gbacore_run_frame`/`gbacore_run_loop` (`gbacore.c:143-153`), on core 0 (worker A) and core 2 (worker B).\n- The **consumer** is `gbacore_read_audio`→`mAudioBufferRead` and `gbacore_drain_audio`→`mAudioBufferClear` (`gbacore.c:171-177`).\n- **Both mutate the same `buffer->size`** non-atomically: write does `buffer->size += length` (`circle-buffer.c:135`), read does `buffer->size -= length` (`circle-buffer.c:241`), clear does `buffer->size = 0` (`circle-buffer.c:43`). There is **no lock inside** these. `mCoreSyncLockAudio` at `audio.c:409` is a no-op here (we run with no `mCoreThread`/sync object).\n\n**Conclusion:** simply moving `audio_feed` to a new thread does NOT make it safe — today it's only safe because the handshake (`main.c:419-425`) guarantees the worker is *parked* on `done` before main reads. A free-running audio thread reading the core ring concurrently with the worker writing it is a data race on `size`/`readPtr`/`writePtr` → exactly the \"buffer race\" the linked path avoids by muting (`main.c:414-415`). So the design must **decouple the consumer from the core ring**.\n\n**The clean decoupling: each worker drains its own core ring into an SPSC handoff; the audio thread consumes only the SPSC.**\nThe worker already owns its core ring exclusively at well-defined points (right after `gbacore_run_frame` in the handshake path; after each `gbacore_run_loop` in the linked path, `main.c:85-90`). Have the worker, immediately after producing a frame, call `mAudioBufferRead` into a **per-core SPSC ring** that the audio thread reads. Now:\n- Core ring: written by worker (in `runFrame`), read by worker (the drain) — **same thread, no race**, even during a link.\n- SPSC ring: written by exactly one worker, read by exactly one audio thread — safe with the standard lock-free SPSC discipline (atomic head/tail, release/acquire). libctru gives you `__dmb()` and `__atomic_*` (`synchronization.h:40-43, 133-141`).\n\nThis is strictly better than a mutex around the core read: a mutex would serialize the audio thread against the *worker's hot path* (the worker would have to take it inside/around `runFrame`, or the audio thread blocks the worker mid-scene) — reintroducing exactly the coupling we're removing. The SPSC drain is a `memcpy`-sized, contention-free handoff. (A mutex is acceptable as a fallback only if you accept the audio thread briefly blocking a worker; not recommended.)\n\n## ndsp ownership and pacing\n- ndsp is one stereo channel (channel 0), PCM16, set up in `audio_init` (`audio.c:28-40`). The **audio thread becomes the sole owner** of channel 0: only it calls `ndspChnWaveBufAdd`/`ndspChnWaveBufClear`. Main must stop calling `audio_feed` (`main.c:425`) and `audio_reset_stream` directly mid-session (it instead posts a request to the audio thread — see recommendation).\n- **Pacing primitive:** `ndspSetCallback` (`ndsp.h:174`) fires once per DSP audio frame (~5 ms). The libctru ogg-vorbis example is the canonical pattern: the callback does `LightEvent_Signal(&s_event)`; the audio thread loops `{ refill any free wave buffers; LightEvent_Wait(&s_event); }` (example `audioCallback`/`audioThread`, lines 206-237, 288). This wakes the audio thread exactly when the DSP needs more data — no busy-poll, no dependence on the render frame. Use `LightEvent_Init(..., RESET_ONESHOT)` like the worker events (`main.c:102`).\n- **Wave-buffer sizing to avoid underrun (\"smoosh\"):** current `AUDIO_FRAMES=1280`, `AUDIO_DSP_BUFS=4` (`audio.c:7-8`) = 4×~26 ms ≈ 107 ms of buffering. That depth is fine; the dropouts today are because the *only* refill opportunity is once per render frame, and in heavy scenes the render frame stretches past the time the DSP drains a buffer. The ndsp-callback-driven thread fixes that directly: refills happen on the DSP's 5 ms cadence regardless of render time. Keep 4 buffers; you may raise to 5-6 for extra slack at no audible-latency cost that matters for an emulator. Keep each buffer ~10-26 ms.\n\n## Core placement\n- Audio thread on **core 1 (syscore)**: allowed because `APT_SetAppCpuTimeLimit(80)` is already called (`main.c:728`), which is exactly the precondition libctru documents for \"create a single thread on processor #1\" (`thread.h:43-44`). Pass `core_id=1` to `threadCreate`. The work is light (drain SPSC + int16 mix + `ndspChnWaveBufAdd` ≈ a few thousand samples per 5 ms), well within the syscore slice. Priority: `mainPrio-1` (one higher than main, like the ogg example's `priority-1`), so it preempts render briefly to refill, then sleeps.\n- This frees core 0 (main render + worker A) and core 2 (worker B) of all audio work — that is the 30→60 fps recovery, since the measured symptom is \"audio path is the dominant cost in hard scenes\" and that cost currently lands on the main/render thread (`main.c:425`).",
          "recommendation": "Add a dedicated audio thread pinned to core 1, fed by per-core lock-free SPSC rings that the worker threads drain. Concretely:\n\n**1. Per-core SPSC ring (new, in audio.c), one per EmuInstance.** Capacity ~8192 stereo frames (power-of-two). Fields: `int16_t* buf; u32 cap; volatile u32 head; volatile u32 tail;` (head=write index owned by worker, tail=read index owned by audio thread). Single producer (the worker), single consumer (the audio thread). Use `__atomic_load_n(&x, __ATOMIC_ACQUIRE)` / `__atomic_store_n(&x, v, __ATOMIC_RELEASE)` on head/tail; the libctru atomics in synchronization.h cover this. No mutex.\n\n**2. Worker drains its core into its SPSC, right where it already owns the ring.** In `worker_main`:\n   - Unlinked path: after `emu_step(e)` (main.c:92), call `audio_pump_core(e)` which does `gbacore_read_audio(e->core, tmp, n)` then writes into `e->spsc`. This runs *before* `LightEvent_Signal(&e->done)`, so it's the same single-threaded ownership window you have today.\n   - Linked path: after each `gbacore_run_loop` (main.c:87-88), same `audio_pump_core(e)`. Because the worker both produces (inside runLoop) and drains (right after), the core ring is never touched cross-thread — **this is what lets audio survive a link**: remove the `audio MUTED while linked` behavior and the upload-only branch's silence (main.c:414-417).\n   - If the SPSC is full (audio thread fell behind), drop the oldest by advancing tail-equivalent or just skip the write (favor latest) — never block the worker.\n\n**3. Audio thread (new, on core 1, prio mainPrio-1) owns ndsp channel 0.** Loop:\n   ```\n   ndspSetCallback(audio_cb, NULL);            // signals s_audioEv each DSP frame (~5ms)\n   while (!g_quit) {\n     // for each free wave buffer (status != QUEUED/PLAYING):\n     //   read from both SPSC rings (consumer side), apply solo/mixed/split + volA/volB\n     //   (reuse the exact mix math in audio.c:101-122), ndspChnWaveBufAdd\n     LightEvent_Wait(&s_audioEv);\n   }\n   ```\n   Move all of audio_feed's mixing into this consumer; it now reads SPSC rings, not core rings. Mode/volume/focus are read from atomically-published volatile fields the menu sets (audioMode/volA/volB/focused already exist as plain vars in run_session — publish them via a small `audio_set_params()` call instead of passing to audio_feed).\n\n**4. Main loop changes:** delete the `audio_feed(...)` call (main.c:425). `audio_reset_stream` on focus switch / mode change (main.c:369,467) becomes a flag the audio thread honors at its next wake (clear ndsp queue + clear both SPSC rings on the consumer side; workers keep filling) so main never touches ndsp.\n\n**5. ndsp pacing:** keep AUDIO_DSP_BUFS=4 (raise to 6 if you still hear gaps), AUDIO_FRAMES ~640-1280 per buffer. The callback-driven refill is the underrun fix; do NOT pace the audio thread off the render frame or a sleep — wait on the ndsp callback event.\n\n**6. Lifecycle:** create the audio thread in run_session after cores are set up; on teardown set g_quit, `LightEvent_Signal(&s_audioEv)` to wake it, `ndspSetCallback(NULL,NULL)`, threadJoin/threadFree before the cores are destroyed (so it never reads a freed SPSC). Keep audio_init/audio_exit (ndspInit/ndspExit) at app scope in main() as today.\n\nThis keeps the pure-C boundary intact (SPSC + mix live in audio.c, no mGBA/citro types), takes 100% of audio off the render/worker hot paths, and makes the linked case audible because each core's ring is only ever touched by its own worker.",
          "confidence": "HIGH",
          "risks": "- **Core-1 syscore budget:** the audio thread on core 1 shares the syscore slice with system services (and is gated by APT_SetAppCpuTimeLimit(80), main.c:728). The work is light, but if you ever see audio-thread starvation, the fallback is core_id=-1 (any core) or pin to core 0 at high prio. Must be confirmed on real New 3DS (the \"Done gate\" in CLAUDE.md) — Azahar does not model syscore contention or the ndsp callback cadence faithfully.\n- **SPSC correctness on weak memory:** ARM11 is weakly ordered. The head/tail publish MUST use the release/acquire atomics shown; a naive `volatile` without barriers can let the consumer read stale buffer contents past a stale index. This is the one place to get exactly right; the libctru __atomic_* macros (synchronization.h:133-141) and __dmb (line 40) are sufficient.\n- **Rate/clock matching unchanged:** ndspChnSetRate is still derived per core via gbacore_ndsp_rate (gbacore.c:160-165, audio.c:59-62). The two cores share one rate today; under free-run (link) they can drift. The SPSC decoupling means the audio thread consumes whatever each core produces — if one core runs slightly fast/slow during a link, its SPSC will slowly fill/drain. Mitigate with the existing \"don't let cores drift\" drain (audio.c:124-125) applied to the SPSC fill level (drop oldest when a ring exceeds ~2 buffers), so a drifting linked core trims rather than overflows.\n- **One DSP channel, mixed in software:** keeping a single stereo channel (vs two ndsp channels) is the right call — it preserves the solo/mixed/split modes (which need cross-core combine) and avoids per-channel rate/sync issues. No risk, just noting the design intentionally does NOT split to two ndsp channels.\n- **dspfirm dependency unchanged:** still silent without dspfirm.cdc (audio.c:29 / memory note \"audio-needs-dspfirm\"); the redesign doesn't change that prerequisite."
        },
        "verify": {
          "verdict": "CONFIRMED",
          "corrections": "All key claims hold; no corrections needed",
          "notes": "Core placement clock path the mGBA non-atomic circle buffer race the per-core SPSC fix ndsp callback pacing and the root cause all verified against the libctru headers app rsf gbacore source circle buffer source and 3dbrew Validate on real New 3DS"
        }
      },
      {
        "key": "speedup-detect",
        "research": {
          "report": "## Detecting & warning when N3DS 804MHz + L2 is NOT active\n\n### 1. The clock lever — what actually turns 804MHz + L2 on\n\n**`osSetSpeedupEnable(true)` is the real runtime lever, NOT the exheader flag.** Authoritative chain:\n\n- libctru `osSetSpeedupEnable(bool)` (`/opt/devkitpro/libctru/include/3ds/os.h:331`) just sets a flag and calls `__ctru_speedup_config()`, which calls **`PTMSYSM_ConfigureNew3DSCPU(__ctru_speedup ? 3 : 0)`** (devkitPro/libctru `source/os.c`). The value **3 = bit0 (EnableL2Cache) | bit1 (cpuspeed_804MHz)** — exactly the two exheader Flag1 bits from 3dbrew NCCH/Extended_Header (Flag1 bit0 = `EnableL2Cache` New3DS-only, bit1 = `cpuspeed_804MHz`).\n- `PTMSYSM:ConfigureNew3DSCPU` \"uses svcKernelSetState with Type10 … the above type is **only implemented in the New3DS ARM11-kernel**\" (3dbrew PTM Services). So on Old 3DS the call is a harmless no-op; on New 3DS it flips the clock+L2 live. **libctru does NOT gate the call on `APT_CheckNew3DS`** — it just calls it (no-op on O3DS), so calling it unconditionally is safe.\n\n**Does the .cia RSF flag guarantee 804MHz?** No — and this is the crucial, counter-intuitive finding. 3dbrew: \"Homebrew which runs under a title which has the cpuspeed flag set runs much faster on New3DS,\" BUT \"when launching titles with APT, NS uses PTMSYSM:ConfigureNew3DSCPU with data from these flags, **due to a bug(?) in NS the value sent to that command is always either 0x0 or 0x3**.\" And per smea (the homebrew-launcher author, widely cited): \"By default the higher clockrate is no longer enabled, and developers need to … add `osSetSpeedupEnable(1)`.\" So the exheader flag only grants *permission/capability*; the app must still **call `osSetSpeedupEnable(true)` at runtime** (which `source/main.c` already does — good). The .cia exheader is what makes that call actually succeed in raising the clock.\n\n**Luma3DS caveat (real regression):** LumaTeam/Luma3DS issue #996 reports that in Luma v9.0, a homebrew title with `CpuSpeed:804MHz`/`EnableL2Cache:true`/`124MB` RSF \"no longer boot[s] with those settings enabled\" — it came up at 268MHz with L2 off, even with \"New 3DS CPU\" enabled in Luma config, where v8.1.1 worked. It was closed \"invalid/external,\" but it demonstrates that **the firmware/CFW environment can override the exheader**, which is exactly why a runtime *empirical* detector (below) is needed rather than trusting the flag.\n\n**Running the .3dsx from the Homebrew Launcher (HBL):** HBL launches homebrew *inside another title's sandbox* (historically the Download Play / a host title's process), so the .3dsx **inherits the host title's exheader and clock state — typically 268MHz, no L2, no core 2.** A `.3dsx` cannot set its own exheader CPU flags, so even calling `osSetSpeedupEnable(true)` from HBL is unreliable — it may be denied or no-op depending on the host. This matches the CLAUDE.md convention #1 (\"a `.3dsx` from the Homebrew Launcher can't reliably claim core 2\"). The **only** reliable way to get 804MHz + L2 + core 2 for this project is to **install and run the `.cia`.**\n\n**The actual user-facing lever:** (a) Install + run the `.cia` (not the .3dsx). (b) On real hardware, Luma3DS's config menu has a \"New 3DS CPU\" / N3DS-speedup option that should be left in its default that allows per-title flags (forcing it globally can interfere, per issue #996). There is no other \"turn on accelerations\" switch the user controls; it is not something they enable per-launch.\n\n### 2. CRITICAL: svcGetSystemTick is FIXED at 268MHz — it does NOT scale to 804MHz\n\nDefinitive, two independent confirmations:\n- **3dbrew Hardware page:** \"svcGetSystemTick uses a tick counter running at **268MHz** in this mode\" — independent of the variable CPU clock.\n- **libctru os.h:16-17:** `SYSCLOCK_ARM11` is documented as \"The clock rate for the Arm11 **in CTR mode and in svcGetSystemTick**\" = `SYSCLOCK_ARM9*2` = **268,111,856 Hz**. The N3DS clocks are separate constants (`SYSCLOCK_ARM11_LGR2` = `SYSCLOCK_ARM11*3` ≈ 804MHz) and are NOT what the tick counter uses.\n\n**Consequence (a) — your HUD is CORRECT.** `osTickCounterRead` divides elapsed ticks by `CPU_TICKS_PER_MSEC` = `SYSCLOCK_ARM11/1000.0` (os.h:25; impl `os.c`: `elapsed / CPU_TICKS_PER_MSEC`). Because the tick source is the fixed 268MHz counter regardless of whether the CPU is at 268 or 804, dividing by SYSCLOCK_ARM11 yields correct wall-clock milliseconds in BOTH modes. Your worst-frame-ms HUD does NOT need a correction factor for N3DS-804. (Sub-tlety: libctru's `osGetTime` instead divides by PTM's RTC-calibrated `tr.sysclock_hz` (os.h:106, \"usually around SYSCLOCK_ARM11\"), which is still the ~268MHz tick rate, just drift-corrected — also unaffected by CPU clock.)\n\n**Consequence (b) — you CANNOT detect 804MHz by comparing svcGetSystemTick to osGetTime.** Both are driven by the *same* ~268MHz tick counter (osGetTime is literally `(svcGetSystemTick()-value_tick)*1000/sysclock_hz`). They track each other 1:1 in every clock mode, so their ratio is a constant ≈1.0 whether the CPU is at 268 or 804. That comparison detects nothing about CPU clock.\n\n### 3. Empirical detector (since there is no getter)\n\nThe correct method compares **CPU work accomplished** against **the fixed-rate tick/wall clock**. A fixed-iteration busy loop completes ~3× faster at 804MHz than at 268MHz, while the tick counter runs at the same 268MHz rate in both — so wall-clock time (or tick delta) for the same work is the discriminator.\n\nRecipe:\n```c\n// Calibrated busy loop; volatile to defeat the optimizer.\nstatic u64 busy_ticks(void) {\n    volatile u32 x = 0;\n    u64 t0 = svcGetSystemTick();\n    for (volatile u32 i = 0; i < 8000000u; i++) x += i;   // ~tens of ms of pure ALU work\n    return svcGetSystemTick() - t0;                        // ticks @ fixed 268MHz\n}\n// Run AFTER osSetSpeedupEnable(true), on the MAIN thread, before workers start,\n// with nothing else competing. Optionally take the min of 2-3 runs.\nu64 d = busy_ticks();\ndouble ms = (double)d / CPU_TICKS_PER_MSEC;\n```\nCalibration / threshold: the loop runs ~3× faster at 804MHz, so `ms` (and the raw tick delta `d`) at 804 is roughly **1/3** of the 268MHz value. Don't hardcode an absolute ms (compiler/loop-body dependent) — **calibrate once on your build**: print `d` on a known-268 boot and a known-804 boot, then set the threshold at the geometric midpoint, e.g. classify \"fast\" if `d < 0.55 * d_268` (equivalently `d` near `d_804`). A robust ratio test: 804/268 = 3.0, so midpoint ≈ 1.73× the fast value → `is804 = (d * 173 < d_268_calibrated * 100)`. Run the loop only on the main core (avoid worker contention) and ignore the result on Old 3DS (where `APT_CheckNew3DS` is false — there is no 804 mode at all).\n\n**`APT_GetAppCpuTimeLimit(u32*)`** (`/opt/devkitpro/libctru/include/3ds/services/apt.h:521`) confirms a *different* thing: how much of the **syscore (core 1) CPU time** you were granted (your code sets 80% via `APT_SetAppCpuTimeLimit(80)`). It does NOT report clock speed or core-2 availability and is unaffected by 804MHz. Use it only to verify the syscore slice was granted (read it back == 80); it is orthogonal to the speedup detection. Core-2 availability is confirmed separately by `APT_CheckNew3DS(bool*)` (apt.h:527) succeeding with `true` plus your exheader `CanAccessCore2` (a successful `threadCreate` pinned to core 2 is the real proof).\n\n### 4. Recommended startup check + warning text\n\n```c\nbool isNew3DS = false; APT_CheckNew3DS(&isNew3DS);\nosSetSpeedupEnable(true);                 // no-op on O3DS; needs .cia exheader on N3DS\nu32 slice = 0; APT_GetAppCpuTimeLimit(&slice);   // expect 80\nbool clockFast = detect804();             // busy-loop detector above\n\nif (!isNew3DS) {\n    WARN(\"Old 3DS detected. Two GBA cores need a New 3DS (804MHz + L2 + core 2). Expect heavy slowdown.\");\n} else if (!clockFast) {\n    WARN(\"New 3DS detected, but running SLOW (268MHz / no L2 / no core 2).\\n\"\n         \"You are likely running the .3dsx from the Homebrew Launcher.\\n\"\n         \"Install and run the .CIA for full speed (804MHz + L2 + core 2).\");\n} else {\n    // 804MHz confirmed; optionally also assert the core-2 worker thread was created.\n}\n```",
          "recommendation": "Implement a startup self-check, do NOT trust the RSF flag. Concretely:\n\n1. KEEP the existing `osSetSpeedupEnable(true)` call — it is the real runtime lever (it calls `PTMSYSM_ConfigureNew3DSCPU(3)` = L2-bit | 804MHz-bit). The exheader flags only grant *permission*; the runtime call is what flips the clock. It is a safe no-op on Old 3DS, so call it unconditionally.\n\n2. DO NOT change the worst-frame-ms HUD. svcGetSystemTick is FIXED at 268MHz (SYSCLOCK_ARM11) in all clock modes, so dividing by SYSCLOCK_ARM11 / CPU_TICKS_PER_MSEC is correct at both 268 and 804MHz. No N3DS correction factor is needed.\n\n3. DO NOT detect 804MHz by comparing svcGetSystemTick to osGetTime — both ride the same 268MHz tick counter and track 1:1, so the ratio is constant. Instead use a calibrated fixed-iteration busy loop: measure its svcGetSystemTick delta; it is ~3x smaller at 804MHz than at 268MHz. Calibrate the threshold once on your own build (geometric midpoint, e.g. classify fast if tick_delta * 173 < calibrated_268_delta * 100).\n\n4. Run the detector once at startup on the main core (no worker contention), gated on APT_CheckNew3DS()==true. Use APT_GetAppCpuTimeLimit only to confirm the syscore slice (==80), not for clock detection.\n\n5. Show the warning: if not New3DS -> \"Old 3DS: needs New 3DS for two cores, expect slowdown\"; if New3DS but slow -> \"New 3DS detected but running SLOW (268MHz/no L2). Likely the .3dsx under Homebrew Launcher. Install and run the .CIA for full speed (804MHz + L2 + core 2).\" The true user lever is: install/run the .cia (a .3dsx under HBL inherits the host's ~268MHz state and cannot reliably claim 804/L2/core2); there is no per-launch \"enable accelerations\" toggle beyond that.",
          "confidence": "HIGH",
          "risks": "- The busy-loop detector needs PER-BUILD calibration (the absolute tick delta depends on the compiled loop body and on memory/cache effects — L2 on vs off can itself add a few % beyond the 3x clock factor). Mitigate by calibrating on a real 268 boot and a real 804 boot and using a ratio threshold, not a hardcoded ms. CLAUDE.md gate #6 applies: verify on real New 3DS, not Azahar (the emulator does not model the 268-vs-804 distinction or L2 faithfully).\n- The exact value mapping (bit0=L2, bit1=804MHz, so 3=both) comes from 3dbrew NCCH/Extended_Header Flag1 bits cross-referenced with libctru passing literal 3; 3dbrew's PTMSYSM:ConfigureNew3DSCPU page itself does not enumerate the bits (it only documents svcKernelSetState Type10, New3DS-kernel-only). Confidence is high via the cross-reference but the per-bit semantics are not stated on one single page.\n- The Luma3DS #996 regression (RSF flags ignored in v9.0) was closed invalid/external and may be version-specific; behavior can differ across Luma versions. This is itself the argument FOR a runtime empirical detector rather than trusting the flag.\n- I could not load the smea GBAtemp thread directly (403); the smea quote (\"developers need to add osSetSpeedupEnable(1)\") is via WebSearch summary of that thread plus the corroborating 3dbrew NS-bug note and the SDL/PTMSYSM commit. The conclusion is consistent across all three.\n- HBL-inherits-host-clock is well-established convention (and matches CLAUDE.md) but the precise inherited clock depends on which host title HBL is running under; \"typically 268MHz/no L2/no core2\" is the common case, not a guarantee — which is exactly why the empirical detector (not assumptions) should drive the warning."
        },
        "verify": {
          "verdict": "CORRECTED",
          "corrections": "The analysis is overwhelmingly correct; every load-bearing hardware claim holds. One factual attribution needs correcting, and a few nuances should be tightened.\n\nCORRECTION 1 (the only real error) — per-bit meaning of the ConfigureNew3DSCPU value. The analysis says: \"value 3 = bit0 (EnableL2Cache) | bit1 (cpuspeed_804MHz)\" for PTMSYSM_ConfigureNew3DSCPU, deriving the command's bit layout from the EXHEADER Flag1 bit layout. Those are two different fields. The actual runtime command's bit order is the OPPOSITE per the local header:\n- /opt/devkitpro/libctru/include/3ds/services/ptmsysm.h:131-134 PTMSYSM_ConfigureNew3DSCPU docstring: \"@param value Bit0: enable higher clock, Bit1: enable L2 cache.\" -> for the COMMAND, bit0=804MHz clock, bit1=L2 (NOT bit0=L2/bit1=804).\n- 3dbrew NCCH/Extended_Header Flag1 (the EXHEADER bits, a different field): bit0=EnableL2Cache, bit1=cpuspeed_804MHz -> this matches what the analysis cited, but it is the exheader layout, not the command's.\nImpact on the conclusion: NONE for the headline. value 3 = 0b11 sets both bits whichever ordering you use, so \"3 enables both 804MHz + L2\" is correct. Moreover the 3dbrew NS bug (confirmed verbatim) means NS only ever sends 0x0 or 0x3 anyway. So the recommendation stands; only the parenthetical \"bit0=EnableL2Cache, bit1=cpuspeed_804MHz\" attribution applied to the PTMSYSM command is wrong. Fix the wording to: exheader Flag1 is bit0=L2/bit1=804MHz; the PTMSYSM_ConfigureNew3DSCPU u8 value (per libctru) is bit0=clock/bit1=L2; either way value 3 means both on.\n\nCORRECTION 2 (nuance, not wrong) — the NS bug actually strengthens the case and is worth stating precisely. 3dbrew (NCCH/Extended_Header) verbatim: \"due to a bug(?) in NS the value sent to that command is always either 0x0 or 0x3. When calculating that value, the code only ever uses the cpuspeed field, not the cache field.\" So with the cpuspeed/804MHz exheader flag set, NS sends 0x3 (both clock AND L2 on, despite the cache field being ignored in the calc); without it, 0x0. This means the exheader cpuspeed flag alone effectively also drives L2 via NS, in addition to the app's own osSetSpeedupEnable(true) call.\n\nCORRECTION 3 (precision) — SYSCLOCK_ARM11 numeric value. The analysis states 268,111,856 Hz. Verified by the header chain: SYSCLOCK_SOC=16756991; *2(SYS) *2(SDMMC n/a) -> ARM9 = SYSCLOCK_SOC*2*4 = 134,055,928; ARM11 = ARM9*2 = 268,111,856 Hz. Correct. SYSCLOCK_ARM11_LGR2 (the ~804MHz \"NEW\" rate) = ARM11*3 = 804,335,568 Hz (os.h:21,23). These are nominal libctru constants, not exact silicon; fine for the argument.",
          "notes": "CONFIRMED key claims (header + 3dbrew cross-checked):\n\n1. osSetSpeedupEnable(bool) is the runtime lever, setter-only, calls PTMSYSM_ConfigureNew3DSCPU. os.h:328-331 (\"Configures the New 3DS speedup\", void osSetSpeedupEnable(bool enable)); ptmsysm.h:134 PTMSYSM_ConfigureNew3DSCPU(u8 value). Source os.c not installed locally (only headers present), but the call chain is corroborated by the smea/GBAtemp note that devs must add osSetSpeedupEnable(true).\n\n2. ConfigureNew3DSCPU = svcKernelSetState Type10, New3DS-ARM11-kernel-only, \"implemented in the Old3DS PTM sysmodule too, but the above type is only implemented in the New3DS ARM11-kernel\" (3dbrew PTMSYSM:ConfigureNew3DSCPU). So calling unconditionally is safe (no-op effect on O3DS). CONFIRMED.\n\n3. NS bug \"value always 0x0 or 0x3\" — CONFIRMED verbatim on 3dbrew NCCH/Extended_Header (see Correction 2).\n\n4. smea: \"By default the higher clockrate is no longer enabled... developers need to ... add osSetSpeedupEnable(1)\" — CONFIRMED (GBAtemp thread 401108).\n\n5. svcGetSystemTick is FIXED at 268MHz independent of CPU clock — CONFIRMED. 3dbrew Hardware: \"svcGetSystemTick uses a tick counter running at 268MHz in this mode.\" os.h:16 SYSCLOCK_ARM11 doc: \"The clock rate for the Arm11 in CTR mode and in svcGetSystemTick.\" Therefore: (a) the worst-frame-ms HUD using CPU_TICKS_PER_MSEC=SYSCLOCK_ARM11/1000.0 (os.h:25) is correct at BOTH 268 and 804 — no N3DS correction factor. CONFIRMED. (b) You CANNOT detect 804MHz by svcGetSystemTick-vs-osGetTime ratio — osGetTime/osTimeRef ride the same ~268MHz tick (os.h:106 sysclock_hz \"usually around SYSCLOCK_ARM11\", RTC-calibrated). CONFIRMED.\n\n6. Empirical busy-loop detector (same work finishes ~3x faster at 804MHz vs fixed-rate tick) is the correct method; needs per-build calibration + ratio threshold; run on main core gated on APT_CheckNew3DS()==true. Sound. CONFIRMED. Caveat the analysis already flags: L2-on adds a few % beyond the pure 3x clock factor, which is why a ratio/midpoint threshold (not a hardcoded ms) is right.\n\n7. APT decls — apt.h:515 SetAppCpuTimeLimit(u32), apt.h:521 GetAppCpuTimeLimit(u32*), apt.h:527 CheckNew3DS(bool*). GetAppCpuTimeLimit reports the SYSCORE(core1) slice, orthogonal to clock. CONFIRMED.\n\n8. CORE LAYOUT (the costly ones flagged for special attention) — CONFIRMED from thread.h:42-46 docstring AND 3dbrew Multi-threading:\n   - Core 0 (appcore): always usable.\n   - Core 1 (syscore): \"If APT_SetAppCpuTimeLimit is used, it is possible to create a SINGLE thread on this core\" (thread.h:43). So a heavy worker CAN live on core 1, but only ONE thread, and it shares syscore with the OS — and 3dbrew warns high time-limit can yield poorer perf if the app leans on system services. So core 1 is usable for one extra heavy thread but is the riskiest core for a 60fps budget.\n   - Core 2 (New3DS): usable with exheader kernel-flag bit13 = 0x2000 (= CanAccessCore2). thread.h:44 + 3dbrew (bit \"13 - Process has access to CPU core 2\"). CONFIRMED.\n   - Core 3 (New3DS): \"Normal applications cannot create threads on this core\" (thread.h:45). 3dbrew Multi-threading: \"When processorid==0x3 and the process is not a BASE mem-region process, error 0xD9001BEA is returned.\" CONFIRMED UNUSABLE for this homebrew (it is not a BASE-region process). => For the user's \"use more of the 4 cores\" question: realistically only cores 0, 1, 2 are available; core 3 is OFF the table. The headroom for splitting graphics/audio is core 1 (one extra thread, with caveats) — NOT core 3.\n\n9. Luma3DS #996 regression (RSF CpuSpeed:804MHz/EnableL2Cache ignored in v9.0, booted at 268MHz/no-L2, worked in v8.1.1) — CONFIRMED real (GitHub LumaTeam/Luma3DS#996). This is the argument FOR a runtime empirical detector, exactly as the analysis says. Note it was version-specific/closed; not necessarily current behavior.\n\n10. .3dsx under HBL inherits host clock (typically 268/no-L2/no-core2), .cia is the only reliable path to 804+L2+core2 — consistent with CLAUDE.md convention and the smea note; not a single-source guarantee (depends on host title), but the right default assumption. The empirical detector (not the assumption) should drive the warning.\n\nBOTTOM LINE: The detection-and-warning strategy is sound and safe to implement as recommended. Keep osSetSpeedupEnable(true) unconditional; keep the HUD ms math unchanged; use the busy-loop detector (calibrated), not the tick-vs-time ratio; gate on APT_CheckNew3DS. Only fix the bit0/bit1 wording (Correction 1). Relevant files cited: /opt/devkitpro/libctru/include/3ds/os.h (lines 9-26 sysclock defs, 95-110 osTimeRef_s, 255-290 tick counters, 328-331 osSetSpeedupEnable), /opt/devkitpro/libctru/include/3ds/services/ptmsysm.h (124-134), /opt/devkitpro/libctru/include/3ds/svc.h (1149 svcGetSystemTick, 1187 svcKernelSetState), /opt/devkitpro/libctru/include/3ds/services/apt.h (515-527), /opt/devkitpro/libctru/include/3ds/thread.h (30-51 core docstring)."
        }
      }
    ]
  }
}